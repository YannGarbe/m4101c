Création d'un serveur web en C, le mygaServer.

La compilation se faire par l'intermédiaire d'un Makefile dans le répertoire root :
Pour compiler, lancez la commande "make".
La make commande 'make' peut se lancer soit dans le répertoire racine, soit dans le répertoire 'webserver'.

L'éxécutable s'appelle "mygaServer" qui s'éxécute avec la commande "./mygaServer" dans le répertoire 'webserver'.

-Lorsque le ou les clients  se connecte(nt) au serveur, il(s) reçoit/reçoivent un message de bienvenue.
-Le client peut envoyer des messages au serveur qu'il renvoie de nouveau au client.
